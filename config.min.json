{
  "_comment": "Minimal required configuration - Use combined_credentials_file instead",
  "auth": {
    "combined_credentials_file": "./credentials.json",
    "_combined_credentials_file": "Path to JSON file with auth and vault passwords"
  },
  "_credentials_format": "See credentials.example.json for the required format"
}